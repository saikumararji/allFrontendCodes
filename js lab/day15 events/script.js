let colorString = "ABCDEF0123456789"

function generateColor(){
    let color = "#";
    for(let index = 0;index<6;index++){
        color+= colorString[Math.floor(Math.random)]
    }
}

// BigInt.addEventListener('click')